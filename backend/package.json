{
  "name": "piastock",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npm install --omit=dev && node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.15"
  },
  "engines": {
    "node": "18.x"
  }
}
